ci-test:
  base_definition:
    traits:
      component_descriptor: ~
      version: ~
  jobs:
    gh-config-test:
      repo:
        trigger: false
      steps:
        foo:
          publish_to:
          - source
    release:
      traits:
        version:
          preprocess: 'finalize'
        release:
          nextversion: 'bump_minor'
          rebase_before_release: true
      steps:
        foo:
          publish_to:
          - source
    create-upgrade-pr:
      repo:
        trigger: false
      traits:
        update_component_deps: ~

